# Custom DERP map for Hyper-DERP testing.
# Only contains our local relay — no Tailscale public servers.
regions:
  900:
    regionid: 900
    regioncode: "hd-local"
    regionname: "Hyper-DERP Local"
    nodes:
      - name: "hd1"
        regionid: 900
        hostname: "relay.test.local"
        ipv4: "10.101.0.1"
        derpport: 3340
        stunport: -1
        stunonly: false
        insecurefortest: true
